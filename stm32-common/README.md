# STM32-Common

This folder provides utilitary code common to my STM32 drivers.

The header errors.h declare the enum type used to return error types in functions.
The files console.h and console.c enable displaying data through serial with the MCU.